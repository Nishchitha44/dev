version: '3.8'

services:

  flaskapp:
    image: shwethapmurthy/flask-app2:latest   # Your Docker image
    ports:
      - "5000:5000"

    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
